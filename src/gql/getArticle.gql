

query getArticle ($id: String) {
  getArticle (id: $id) {
      id
      title
      shareDesc
      shareTitle
      content
      createAt
      updateAt
      url
      author {
        id
        avator
        name
        desc
        tag {
          text
        }
      }
  }
}
